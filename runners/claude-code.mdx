---
title: "claude-code"
description: "Run Claude Code as an external CLI binary instead of the in-process SDK."
---

The `claude-code` runner invokes the `claude` CLI binary as a subprocess rather than using the SDK in-process. This produces the same model output as `claude-sdk` but runs in a separate process, which can be useful for isolation, reproducibility, or when you want full control over the `claude` binary version independently of the RemoteAgent package.

## Requirements

- The `claude` CLI installed globally:

  ```bash
  npm install -g @anthropic-ai/claude-code
  ```

- Authentication — either a **long-lived OAuth token** (recommended) or an interactive login via `claude login`. See [Authentication](#authentication) below.
- Node.js 20 or later.

Verify the binary is available:

```bash
claude --version
```

## Setup

```bash
remoteagent init --runner claude-code
```

Or choose `claude-code` when prompted by the interactive wizard.

## Authentication

The `claude` binary must be authenticated before the agent can use it. There are two methods:

### Option A — Long-lived OAuth token (recommended)

Generate a token once with:

```bash
claude setup-token
```

This creates a token of the form `sk-ant-oat01-xxxxx...xxxxx` that is **valid for one year** and works on any machine without re-login.

<Warning>
  `claude setup-token` requires an active **Claude Pro or Max** subscription.
  The token is shown only once — copy it immediately before closing the terminal.
</Warning>

During `remoteagent init`, the wizard will ask for this token and store it securely in `~/.remoteagent/agents/{agentId}.json`. The token is injected as the `CLAUDE_CODE_OAUTH_TOKEN` environment variable each time the `claude` binary is invoked — no shell export needed.

### Option B — Interactive login (fallback)

If you do not have a Pro/Max subscription, authenticate with:

```bash
claude login
```

This stores a session credential managed by the `claude` CLI itself. Sessions expire periodically and must be renewed manually by running `claude login` again on the same machine.

<Note>
  The interactive login must be performed on the same machine where the agent runs.
  It is not portable across machines, unlike the OAuth token.
</Note>

## Difference from claude-sdk

Both runners use the same underlying Claude model and produce equivalent output. The key differences are:

| | claude-sdk | claude-code |
|---|---|---|
| Execution | In-process (Node.js) | Subprocess (`claude` binary) |
| Startup overhead | None | ~200–500ms per invocation |
| Process isolation | Shared with agent | Separate OS process |
| Binary version control | Tied to SDK version in package | Independent — update with `npm update -g @anthropic-ai/claude-code` |
| Error output | Structured SDK errors | Raw CLI stderr |

For most users, `claude-sdk` is the better choice. Use `claude-code` if you specifically need process isolation or want to pin the `claude` binary to a particular version.

## How it works

The runner executes:

```bash
claude -p "<prompt>" --dangerously-skip-permissions
```

The `--dangerously-skip-permissions` flag is required because the agent runs non-interactively — there is no user present to approve individual tool calls. RemoteAgent runs this on your own machine, so the permissions boundary is the same as running `claude` manually.

Output is captured from stdout and streamed back to the Redis output channel chunk by chunk.

## Updating the binary

```bash
npm update -g @anthropic-ai/claude-code
claude --version
```

Restart the agent after updating:

```bash
remoteagent start
```
